# VAT - Video Handling & Translation
# 视频翻译自动化流水线依赖

# 核心依赖
PyYAML>=6.0
click>=8.0.0
colorlog>=6.7.0
tqdm>=4.65.0
tabulate>=0.9.0

# 视频下载
yt-dlp>=2023.10.0

# 语音识别
faster-whisper>=0.10.0
# openai-whisper>=20231117  # 可选：使用原版whisper

# 翻译相关
openai>=1.0.0
requests>=2.31.0
tenacity>=8.2.0
tiktoken>=0.5.0
opencc-python-reimplemented>=0.1.7

# LLM 增强功能
json-repair>=0.25.0      # JSON 修复
diskcache>=5.6.0         # LLM 响应缓存
langdetect>=1.0.9        # 语言检测（用于 ASRData）

# 音频处理（用于 ChunkedASR）
pydub>=0.25.0

# 视频处理
# ffmpeg-python>=0.2.0  # 可选：Python封装

# B站上传
biliup>=0.4.0

# 数据库
# 使用Python内置的sqlite3，无需额外依赖

# 工具库
python-dateutil>=2.8.0
croniter>=1.3.0            # Cron 表达式解析（定时上传）

# GalTransl 依赖
httpx<0.28
packaging
prompt_toolkit
pysrt

# WebUI（可选，仅运行 Web 管理界面时需要）
fastapi>=0.115.0
uvicorn>=0.23.0
jinja2>=3.1.0
python-multipart>=0.0.6
